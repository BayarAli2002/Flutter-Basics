class info {
  //this is not a Widget it's a class of type info
  //here String? means that the variable can be null optional parameter
  String name;
  int height;
  DateTime date;

  info({required this.name, required this.height, required this.date});
}
